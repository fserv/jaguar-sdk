This is an example of multi-modal support for images and texts. You can query the jaguar vector
store by text or image.

To set up the environment, run:

    ./setup.sh

which requires sudo to install jaguardb docker package.


To run the program:

    python3 simple_rag.py 'http://127.0.0.1:8080/fwww/'

    (replace 127.0.0.1 with your IP address of http server)

